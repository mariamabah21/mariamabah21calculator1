var form = document.getElementById("formCalculator");

form.addEventListener("click", function (event) {
  {
    console.log(event.target.value);
  }
});
// If the user clicked one of the numbers or operation signs
// just add this number or sign to the input where you show results.
